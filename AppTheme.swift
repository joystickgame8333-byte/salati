import SwiftUI

enum AppTheme {
    static let backgroundTop = Color(red: 0.02, green: 0.04, blue: 0.05)
    static let backgroundMiddle = Color(red: 0.02, green: 0.10, blue: 0.08)
    static let backgroundBottom = Color.black

    static let emerald = Color(red: 0.02, green: 0.55, blue: 0.36)
    static let emeraldDark = Color(red: 0.00, green: 0.24, blue: 0.18)
    static let gold = Color(red: 0.95, green: 0.73, blue: 0.34)
    static let softGold = Color(red: 1.00, green: 0.86, blue: 0.55)

    static let glassLight = Color.white.opacity(0.10)
    static let glassBorder = Color.white.opacity(0.14)
}
